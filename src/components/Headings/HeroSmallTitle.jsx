const HeroSmallTitle = ({ children }) => {
  return (
    <div className="text-2xl uppercase text-center mb-10 tracking-widest text-shadow-lg">
      {children}
    </div>
  );
};

export default HeroSmallTitle;
